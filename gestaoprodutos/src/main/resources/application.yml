spring:
  application:
    name: gestaoprodutos
  datasource:
    url: jdbc:h2:mem:gestaoprodutos
    username: sa
    password:
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
    defer-datasource-initialization: true
